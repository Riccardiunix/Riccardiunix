#!/bin/sh
alias la='exa -lah'
alias ls='exa'
alias ll='exa -lh'
alias l='exa'
alias c='clear'
alias m='setsid -f mpv --speed=1.3 --fs --pause > /dev/null 2>&1'
alias s='doas'
alias v='nvim'
alias vim='nvim'
alias sv='doas nvim'
alias ftp='tnftp'
alias rm='trash-put'
alias rf='trash-put'
alias ping='prettyping --nolegend'
alias sudo='doas'
alias mkd='mkdir'
alias mocp='mocp -M ~/.config/moc'
alias gp='git pull'
alias gc='git clone'
alias bc='bc -l'
alias rget='rsync -aruPW orion@10.0.0.9:/data/UniVR ~'
alias rput='rsync -aruPW $HOME/UniVR orion@10.0.0.9:/data'
alias ffmpeg='ffmpeg -vaapi_device /dev/dri/renderD128 -hide_banner'
alias ffprobe='ffprobe -hide_banner'
alias mbsync='mbsync -c $XDG_CONFIG_HOME/isync/mbsyncrc'
alias wget='wget --hsts-file=$XDG_DATA_HOME/wget-hsts'
alias svn='svn --config-dir $XDG_CONFIG_HOME/subversion'
alias diff='diff --color'
alias grep='grep --color'
alias nsxiv='setsid -f nsxiv > /dev/null 2>&1'
alias mp='setsid -f mpv > /dev/null 2>&1'
alias geany='setsid -f geany > /dev/null 2>&1'
alias surf='setsid -f surf > /dev/null 2>&1'
alias pcmanfm='setsid -f pcmanfm > /dev/null 2>&1'
alias zathura='setsid -f zathura > /dev/null 2>&1'
alias monerod='monerod --data-dir $XDG_DATA_HOME/bitmonero'
alias nvidia-settings='nvidia-settings --config="$XDG_CONFIG_HOME"/nvidia/settings'
